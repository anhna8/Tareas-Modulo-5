Este proyecto es un gestor de tareas inspirado en Trello, desarrollado con **JavaScript**, **HTML**, **CSS** y una **API simulada** (`json-server`). Permite **crear, editar, eliminar y organizar** tareas por estado y responsable.

